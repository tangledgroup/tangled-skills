# Security Considerations

When using local web servers, protect API endpoints against CSRF attacks. Enable HTTPS for production applications with sensitive data. Validate all JavaScript input from untrusted sources. See [Security Guide](reference/05-debugging.md) for detailed security recommendations.
